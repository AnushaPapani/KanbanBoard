# Seeded on first boot so existing docs/tests that reference "user"/"password"
# keep working; this is now just one of potentially many real accounts, not
# the only one — real accounts are created via POST /api/signup.
DEMO_USERNAME = "user"
DEMO_PASSWORD = "password"
